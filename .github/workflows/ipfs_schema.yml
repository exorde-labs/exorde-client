name: Generate the json-schema from the models 

on:
  push:
    branches: '**'

jobs:
  run-command:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Generate Schema
        run: pip install -e ./exorde/models ; rm -rf ./exorde/models/schema.json ; python3 exorde/models/__init__.py  >> exorde/models/schema.json
      - name: Display schema
        run: cat exorde/models/schema.json
      - name: Commit changes
        run: |
          git add exorde/models/schema.json
          git config --global user.email "patrick@exordelabs.com"
          git config --global user.name "Patrick Borowy"
          git commit -m 'Automated update of README.md' || echo 'nothing to commit'
          git push https://${{github.actor}}:${{secrets.GITHUB_TOKEN}}@github.com/${{github.repository}}.git HEAD:${{ github.ref }} || echo "no change to push" ; 
